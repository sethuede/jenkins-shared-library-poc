pipeline {
    agent any   
     stages {
            stage('Stage 2') {
                steps {
                checkout scm
                    sh """
                    cd /var/lib/jenkins/workspace/${JOB_NAME}/
                    ls
                    ls -l
                    """
                }
            }
        }
    }
pipeline_initialize()
